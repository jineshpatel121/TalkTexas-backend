# Datasource Configuration
spring.datasource.url=jdbc:postgresql://aws-0-us-east-1.pooler.supabase.com:6543/postgres?sslmode=require
spring.datasource.username=postgres.grtefyldlpkmwabbjtph
spring.datasource.password=MYjinesh121

# JPA / Hibernate Configuration
spring.jpa.hibernate.ddl-auto=create-drop
#pring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Optional: HikariCP Connection Pool Settings
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.pool-name=HikariPool

# Logging Configuration (Optional for Debugging)
logging.level.org.hibernate=DEBUG
logging.level.org.springframework=DEBUG
